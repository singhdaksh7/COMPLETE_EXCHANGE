# Base URLs of the running backend processes.
# User API (src/server.ts) and Admin API (src/admin-server.ts) run as SEPARATE
# processes on separate ports (see backend/.env.example).
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
NEXT_PUBLIC_ADMIN_API_URL=http://localhost:4001/admin/v1
