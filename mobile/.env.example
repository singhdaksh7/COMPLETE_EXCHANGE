# EXORA mobile — environment config.
# Only EXPO_PUBLIC_* vars are inlined into the app bundle. NEVER put secrets here;
# the mobile app talks to the public staging API only.
#
# Copy to `.env` to override the default staging API base.
EXPO_PUBLIC_API_URL=https://dfk68tws8g8oj.cloudfront.net/api/v1
